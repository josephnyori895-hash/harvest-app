{
  "name": "harvestfamily-workers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "cf:provision": "node scripts/provision.mjs",
    "tail": "wrangler tail",
    "test:media": "node scripts/test-media-upload.mjs"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
